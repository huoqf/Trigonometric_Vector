# MathVision 开发路线图 (Roadmap)

基于 PRD 建议的优先级，本路线图规划了 MathVision 的 MVP（最小可行性产品）迭代路径。

## 里程碑 1：基础图形与向量交互引擎 (第一优先级)
**目标**：跑通底层的数学渲染（SVG/Canvas）与数据流（Zustand），实现最核心的几何直觉基础。

*任务拆解：*
1. **基础架构与 Store 初始化**：实现 `useMathState`，支持角度与坐标状态的维护。
2. **单位圆与三角函数定义**：实现可拖拽的单位圆组件，实时展示正弦、余弦、正切数值。
3. **向量基础组件**：实现可拖动首尾的向量组件，支持实时显示长度与方向坐标。
4. **向量加法与分解**：实现三角形法则平行四边形法则的动画；实现基底分解（坐标投影）的交互演示。
5. **点积与投影实验**：实现向量夹角拖拽，同时生成 $\theta \to \vec{a}\cdot\vec{b}$ 变化曲线。
6. **诱导公式对称实验**：实现参考角 $\alpha$ 到 $\pi \pm \alpha$ 的对称映射动画。

## 里程碑 2：高阶组件与综合动态模型 (第二优先级)
**目标**：构建"三屏联动"布局，将单点知识升级为可调整的高阶实验模型，特别是函数图像与复杂三角形模型。

*任务拆解：*
1. **三屏联动 UI 框架**：根据 `ui_rules.md` 开发标准页面模板（左侧操作区、中间图像区、右侧数值区）。
2. **三角函数图像与参数变换**：实现 $y=A\sin(\omega x+\varphi)+b$ 的联动，拖动参数滑块实时更新函数波形。
3. **辅助角波形叠加实验**：实现正弦波与余弦波的实时叠加可视化，直观解释辅助角公式。
4. **解三角形动态调整**：实现通过三个顶点拖拽，实时显示边角及正余弦定理数值的联动实验。
5. **奔驰定理实验**：实现三角形内部动点的系数拆解，动态展示重心与线性组合的最值变化。

## 里程碑 2.5：三角恒等变换与化简系统
**目标**：构建"三角恒等变换与化简系统"，从公式实验到综合应用，全面支持各类代数变换及化简思想。

*任务拆解：*
1. **两角和差公式实验**：实现公式的动态几何解释及直观互动。
2. **二倍角与降次实验**：演示二倍角展开及降次公式在函数波形上的直接反映。
3. **辅助角代数变换**：通过系数分析与波形叠加，直观展示辅助角公式的代数原理及几何意义。
4. **三大统一思想实验**：建立引导式交互，帮助体会"统一角、统一函数名、统一结构/次数"的化简策略。
5. **恒等变换图像对照**：在同一坐标系内对比变换前后表达式的图像，从而直观验证代数恒等性。
6. **高考综合化简训练**：结合高考综合题目，进行化简步骤的拆解与互动训练。

## 里程碑 3：真题训练与错题回收系统 (第三优先级)
**目标**：完成 MVP 商业闭环，将"交互实验"转化为"应试能力"，落地到真题训练中。

*任务拆解：*
1. **题库系统搭建**：确定 JSON 格式规范，录入首批测试用高考真题（解三角形、向量最值）。
2. **渐进式提示面板**：依据 `math_rules.md` 开发包含"苏格拉底提问"的解题思路树组件。
3. **错题复盘系统**：开发 `useRecordStore`，记录做题正误、耗时，并支持一键回溯到相关动画演示。
4. **题目变式与迁移题引擎**：尝试对题目参数进行随机化（在合理几何域内），自动生成同类型变式题。

## 里程碑 4：空间向量与立体几何 (第四优先级)

**目标**：从平面向量迈入三维空间，在**不修改任何现有组件**的前提下，通过新建独立模块扩展系统能力，构建空间向量代数工具体系并打通向量方法在立体几何中的判定与度量应用。

> **框架适配结论（基于 2026-05-25 架构评估）**：
> - ✅ 可直接复用：Zustand Slice 模式、ThreeScreenLayout、KaTeX、Vitest
> - ⚠️ 需扩展（不破坏现有）：类型系统新增 Vector3、App.tsx 导航分组、SVG 补充等轴测投影
> - 🆕 需新建（独立模块）：`math/vector3.ts`、`utils/coordinate3.ts`、`store/useSpaceVectorStore.ts`、`features/space-vector/`
> - ❌ 结构冲突处理：`useMathState` 的极坐标语义不适用三维，通过**新建独立 Store** 解决，严禁修改原 Store

*四阶段推进计划：*

**阶段 0 — 基础设施（先于功能开发）**
- 新建 `src/types/vector3.ts`：定义 `Vector3` 类型与空间向量场景状态接口
- 新建 `src/math/vector3.ts`：三维纯函数库（加减、数乘、模长、点积、叉积、法向量、点面距、异面线距）+ Vitest 单元测试
- 新建 `src/utils/coordinate3.ts`：等轴测投影（3D→2D SVG，无需 Three.js）
- 新建 `src/store/useSpaceVectorStore.ts`：三维向量场景状态（与 `useMathState` 完全隔离）
- 重构 `src/App.tsx` 导航：引入模块分组（平面数学 / 空间几何两级切换），解决 Tab 溢出问题

**阶段 1 — 专题 1-3（SVG 等轴测，概念层）**
1. **空间向量的基础概念与线性运算**：新建 `src/features/space-vector/SpaceVectorBasic.tsx`，等轴测 SVG 演示空间向量加减、数乘与共线判定。
2. **空间向量的坐标表示与运算（代数工具）**：新建 `src/features/space-vector/SpaceVectorCoord.tsx`，坐标输入联动箭头显示与模长/方向角实时演算看板。
3. **空间向量的基本定理与性质（理论核心）**：新建 `src/features/space-vector/SpaceVectorTheorem.tsx`，演示基底分解、共面条件与中点/分点公式空间推广。

**阶段 2 — 专题 4-5（SVG 等轴测，判定与度量层）**
4. **向量方法在立体几何中的应用（几何判定）**：新建 `src/features/space-vector/SolidGeoJudge.tsx`，实现法向量求解、直线平行/垂直/线面角判定实验，拖拽顶点实时更新代数过程。
5. **空间角与距离的计算（度量求解）**：新建 `src/features/space-vector/SolidGeoMetric.tsx`，二面角/线面角/线线角演示，点面距/线面距/异面线距分步演算看板。

**阶段 3 — 专题 6（Three.js 升级，综合层）**
6. **立体几何综合题与微专题（高级综合）**：引入 `three` + `@react-three/fiber`，新建 `src/features/space-vector/SolidGeo3D.tsx` 独立 Canvas 渲染层（不污染现有 SVG 组件）；扩展题库支持正四棱锥/正三棱锥/长方体，配合苏格拉底分步提示系统。

## 远期规划 (MVP 验收后)
- 解析几何、圆锥曲线的可视化拓展。
- 导数最值的动态演示。
- 立体几何 3D 全景渲染（基于里程碑 4，深度引入 Three.js 实现旋转、截面动画）。
- 多维度错题知识图谱。
