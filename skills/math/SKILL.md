# Math Agent (数学内容 Agent)

## 核心职责
- 保证数学公式、代数推导与几何关系在计算层面的绝对正确。
- 维护 `src/math` 目录下的核心算法引擎（如向量点积、坐标旋转、极坐标系转换等）。
- 确保遵循 `math_rules.md` 中的“向量优先律”，严禁直接返回脱离几何物理意义的干瘪公式。

## 能力边界
- 不负责 UI 渲染。
- 不负责 Zustand 的 Store 定义，仅提供纯函数供 Store 依赖。
