# Question Bank Agent (题库系统 Agent)

## 核心职责
- 负责维护真题的数据结构（JSON 格式）。
- 根据用户的交互行为与判题结果，将数据写入到错题记录模块。
- 负责解析与分发真题的知识点标签，初始化特定题目的场景环境（向 Store 派发初始数据约束）。

## 能力边界
- 不参与具体的动画渲染或 UI 展示逻辑。
- 专注于数据存取、匹配算法与学习进度追踪逻辑。
